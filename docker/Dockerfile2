FROM python:3.8-bookworm
LABEL maintainer="Aucke Bos"
# COPY libs/gevent-24.2.1.tar.gz /tmp/gevent-24.2.1.tar.gz
# RUN pip install /tmp/gevent-24.2.1.tar.gz
# RUN pip install gevent

# Install miniconda
ENV PATH="/root/miniconda3/bin:${PATH}"
ARG PATH="/root/miniconda3/bin:${PATH}"
RUN apt-get update

RUN apt-get install -y wget && rm -rf /var/lib/apt/lists/*

RUN wget \
    https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh \
    && mkdir /root/.conda \
    && bash Miniconda3-latest-Linux-x86_64.sh -b \
    && rm -f Miniconda3-latest-Linux-x86_64.sh 
RUN conda --version



RUN conda install gevent

# todo: on windows run `python setup.py bdist_wheel` inside the gevent lib folder